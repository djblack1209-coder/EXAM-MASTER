# 🚀 控制台快速测试代码

## TEST-9.3: 分数更新测试

### ⚡ 最简单方法（推荐）

在排行榜页面的控制台中，直接复制粘贴执行：

```javascript
// 获取页面实例并调用测试方法
getCurrentPages()[getCurrentPages().length - 1].$vm.testUpdateScore(10);
```

---

### 📝 完整测试代码（带日志）

如果需要查看详细日志，使用这个：

```javascript
(async () => {
  console.log('[TEST-9.3] 🧪 开始测试分数更新');
  
  // 获取用户信息
  const userId = uni.getStorageSync('EXAM_USER_ID');
  const userInfo = uni.getStorageSync('userInfo') || {};
  
  if (!userId) {
    console.error('[TEST-9.3] ❌ 用户未登录');
    return;
  }
  
  console.log('[TEST-9.3] 📊 当前用户:', { userId, nickName: userInfo.nickName });
  
  // 计算新分数（当前分数 20 + 10 = 30）
  const currentScore = 20;
  const newScore = currentScore + 10;
  
  console.log('[TEST-9.3] 📤 准备更新分数:', { currentScore, newScore });
  
  try {
    // 调用 API
    const res = await new Promise((resolve, reject) => {
      uni.request({
        url: 'https://nf98ia8qnt.sealosbja.site/rank-center',
        method: 'POST',
        data: {
          action: 'update_score',
          uid: userId,
          nickName: userInfo.nickName || '测试用户',
          avatarUrl: userInfo.avatarUrl || 'https://api.dicebear.com/7.x/avataaars/svg?seed=Guest',
          score: newScore
        },
        success: (res) => {
          if (res.statusCode === 200) {
            resolve(res.data);
          } else {
            reject(res.data || { message: `请求失败: ${res.statusCode}` });
          }
        },
        fail: (err) => {
          reject({ message: '网络请求失败', error: err });
        }
      });
    });
    
    console.log('[TEST-9.3] ✅ 分数更新成功:', res);
    
    // 刷新排行榜
    const pageInstance = getCurrentPages()[getCurrentPages().length - 1].$vm;
    if (pageInstance && pageInstance.loadRankData) {
      await pageInstance.loadRankData();
      console.log('[TEST-9.3] ✅ 排行榜已刷新');
    }
    
    uni.showToast({
      title: `分数已更新: ${currentScore} → ${newScore}`,
      icon: 'success'
    });
  } catch (error) {
    console.error('[TEST-9.3] ❌ 失败:', error);
    uni.showToast({ title: '更新失败', icon: 'none' });
  }
})();
```

---

## 📋 执行步骤

1. **打开排行榜页面**
2. **打开控制台**（开发者工具）
3. **复制上面的代码**（推荐使用最简单方法）
4. **粘贴并执行**
5. **查看结果**：
   - 控制台日志
   - 页面上的分数更新

---

## ✅ 预期结果

- 控制台显示：`[TEST-9.3] ✅ 分数上传成功`
- 页面底部分数从 `20` 更新为 `30`
- 显示 Toast 提示：`分数已更新: 20 → 30`
