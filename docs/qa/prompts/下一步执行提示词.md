# Exam-Master V1.0 后续验证提示词集合

> **用途**：在新会话中继续功能验证工作  
> **使用方式**：复制对应的提示词，粘贴到新会话中开始验证

---

## 🎯 提示词 1: 继续 Module 3 验证（分页加载）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：
- Module 1 (用户登录): ✅ 3/6 项完成
- Module 2 (刷题与解析): ✅ 4/8 项完成  
- Module 3 (错题本): ✅ 2/7 项完成
- Module 4 (AI助教): 🔄 2/5 项完成（UI优化中）
- Module 5 (数据兜底): ✅ 1/6 项完成

**下一步任务：Module 3 剩余测试项**

请执行 **TEST-3.2：错题本分页加载**。

**前置条件**：
- 确保错题本中有超过 20 条错题（如果不足，请先做错更多题目或手动创建测试数据）

**操作步骤**：
1. 进入错题本页面 (`pages/mistake/index.vue`)
2. 观察第一页数据加载（应显示前20条）
3. 滚动到列表底部
4. 观察是否自动触发加载第二页

**预期技术表现**：
- ✅ 滚动到底部时，`loadData(false)` 被调用（`reset = false`）
- ✅ `currentPage` 递增为 2
- ✅ 调用 `storageService.getMistakes(2, 20)`
- ✅ 云函数查询：`skip(20).limit(20)`（第二页）
- ✅ 返回第二页数据
- ✅ `mistakes` 数组追加新数据：`[...this.mistakes, ...result.list]`
- ✅ `hasMore` 更新为新的状态
- ✅ `isLoading` 正确切换（true → false）
- ✅ 页面显示更多错题（总数 > 20）

**提供情报**：
- 复制控制台日志（关注 `[mistake-book]`、`storageService`、分页相关日志）
- 观察UI：列表是否成功追加新数据

**请开始执行。**
```

---

## 🎯 提示词 2: 继续 Module 3 验证（掌握状态更新）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：Module 3 (错题本) - 2/7 项完成

**下一步任务：TEST-3.6 - 更新错题掌握状态**

**操作步骤**：
1. 进入错题本页面
2. 找到任意一道错题
3. 点击"重做此题"按钮
4. 在重做模式下，选择正确答案并提交
5. 观察状态更新

**预期技术表现**：
- ✅ 点击"重做此题"后，`mistake.isPracticing = true`
- ✅ 显示选项列表，允许选择
- ✅ 选择正确答案并提交后，`submitPractice(index)` 判断答案正确
- ✅ 调用 `storageService.updateMistakeStatus(mistakeId, true)`
- ✅ `storageService` 调用云对象 `mistake-book.updateStatus(id, true)`
- ✅ 云函数更新数据库：`{ is_mastered: true, last_practice_time: Date.now() }`
- ✅ 数据库 `exam-mistakes` 表中对应记录的 `is_mastered` 更新为 `true`
- ✅ 同步更新本地缓存：`mistake.is_mastered = true`
- ✅ 页面显示已掌握状态（UI更新）

**提供情报**：
- 复制控制台日志（关注 `[mistake-book]`、`storageService`、云函数调用）
- 观察UI：错题状态是否更新为"已掌握"

**请开始执行。**
```

---

## 🎯 提示词 3: 继续 Module 4 验证（Markdown渲染优化后）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：Module 4 (AI助教) - TEST-4.2 进行中（Markdown渲染优化）

**任务：验证优化后的 Markdown 渲染（Wise 风格）**

AI助教的 Markdown 渲染已优化：
- ✅ 列表使用白色背景，深色文字（不再显示为代码块样式）
- ✅ 粗体文字清晰可见（深色文字 `#1C1C1E`）
- ✅ 代码块浅灰背景，深色文字
- ✅ 整体排版符合 Wise.com 风格

**操作步骤**：
1. 进入 AI 助教页面 (`pages/chat/index.vue`)
2. 发送测试消息："请用列表的形式，总结一下马克思主义的三个组成部分。"
3. 等待AI回复
4. 观察UI显示

**预期结果**：
- ✅ 列表项正确显示为列表样式（带项目符号，白色背景）
- ✅ 粗体文字（如 `**哲学基础**`）在白色背景上清晰可见，不再模糊
- ✅ 整体排版简洁清晰，符合 Wise.com 风格
- ✅ 不再出现黑色背景、绿色文字的代码块样式

**提供情报**：
- 控制台日志（关注 `[Chat]`、AI响应）
- **UI截图或详细描述**：列表样式、粗体文字清晰度、整体排版

**请开始执行。**
```

---

## 🎯 提示词 4: 继续 Module 4 验证（AI服务超时）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：Module 4 (AI助教) - 2/5 项完成

**下一步任务：TEST-4.4 - AI服务超时处理**

**操作步骤**：
1. 进入 AI 助教页面
2. 发送一条消息
3. 在开发者工具中，模拟网络超时（或等待60秒）
4. 观察超时处理

**预期技术表现**：
- ✅ 请求配置了超时：`timeout: 60000`（60秒）
- ✅ 超时后，捕获错误：`timeout` 或网络错误
- ✅ `isTyping` = `false`
- ✅ 显示错误提示：`"网络连接失败"` 或 `"AI 解析请求超时，请稍后重试..."`
- ✅ 不显示AI内容
- ✅ 应用不崩溃

**提供情报**：
- 控制台日志（关注错误捕获、超时处理）
- UI观察：错误提示是否正确显示

**请开始执行。**
```

---

## 🎯 提示词 5: 开始 Module 6 验证（择校功能）

```
你现在是我的"QA 测试工程师"。
请开始验证 Exam-Master V1.0 的**Module 6: 择校功能模块**。

**任务：TEST-6.1 - 择校表单填写流程**

**操作步骤**：
1. 点击底部导航栏进入"择校"Tab (`pages/school/index.vue`)
2. 填写教育背景表单：
   - 本科/专科毕业院校：输入测试学校名称
   - 报考专业：输入测试专业
   - 报考类型：选择"学硕"或"专硕"
   - 目前学历：选择"本科全日制"或"专科/同等学力"
   - 英语证明：从下拉列表选择
3. 点击"✨ 开始 AI 择校匹配"按钮
4. 观察后续流程

**预期技术表现**：
- ✅ 表单数据正确收集到 `formData` 对象
- ✅ 点击按钮后，`submitForm()` 方法被调用
- ✅ Step 2 (目标地区选择) 正确显示
- ✅ 可以选择多个城市
- ✅ Step 3 (能力评估) 滑块正常（英语水平、数学基础）
- ✅ Step 4 (信息确认) 正确汇总前面步骤的数据
- ✅ 提交后触发AI匹配算法（如果有）

**提供情报**：
- 控制台日志（关注表单提交、步骤切换、AI调用）
- UI观察：每个步骤是否正确显示和切换

**请开始执行。**
```

---

## 🎯 提示词 6: 开始 Module 9 验证（排行榜）

```
你现在是我的"QA 测试工程师"。
请开始验证 Exam-Master V1.0 的**Module 9: 排行榜模块**。

**任务：TEST-9.1 - 排行榜数据加载**

**操作步骤**：
1. 进入排行榜页面（通过刷题中心入口或直接导航到 `pages/practice/rank.vue`）
2. 观察数据加载过程

**预期技术表现**：
- ✅ `onLoad` 生命周期执行
- ✅ 调用 `fetchRank()` 方法（如果存在）
- ✅ 调用云函数 `rank-center` 或 `lafService.request('/rank-center', { action: 'get_rank' })`
- ✅ 云函数查询排行榜数据（按分数降序）
- ✅ 返回排行榜列表数据
- ✅ 前三名显示在领奖台区域（特殊样式）
- ✅ 其他排名显示在列表区域
- ✅ 个人排名计算并显示在底部固定区域

**提供情报**：
- 控制台日志（关注 `[rank]`、云函数调用、数据加载）
- UI观察：排行榜是否正确显示（前三名、列表、个人排名）

**请开始执行。**
```

---

## 🎯 提示词 7: 开始 Module 10 验证（PK对战）

```
你现在是我的"QA 测试工程师"。
请开始验证 Exam-Master V1.0 的**Module 10: PK对战模块**。

**任务：TEST-10.1 - PK对战匹配功能**

**操作步骤**：
1. 进入PK对战页面 (`pages/practice/pk-battle.vue`)
2. 观察匹配流程

**预期技术表现**：
- ✅ `gameState` = `'matching'`
- ✅ 显示匹配动画（雷达扫描、头像动画）
- ✅ 调用匹配逻辑（如果有云函数或本地匹配）
- ✅ 找到对手后，`opponentFound` = `true`
- ✅ `opponent` 对象包含对手信息（name, avatar等）
- ✅ 1秒后自动进入对战阶段：`gameState` = `'battle'`

**提供情报**：
- 控制台日志（关注匹配逻辑、状态切换）
- UI观察：匹配动画、对手信息显示、状态切换

**请开始执行。**
```

---

## 🎯 提示词 8: 继续 Module 5 验证（云对象不可用）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：Module 5 (数据兜底机制) - 1/6 项完成

**下一步任务：TEST-5.5 - 云对象不可用时的处理**

**操作步骤**：
1. 模拟云对象初始化失败（可以通过修改代码临时禁用云对象，或检查错误处理逻辑）
2. 尝试调用错题相关功能（保存、获取、删除）

**预期技术表现**：
- ✅ 调用 `uniCloud.importObject('mistake-book')` 时抛出错误
- ✅ 捕获错误：`[StorageService] 云对象初始化失败，将使用本地存储: xxx`
- ✅ `getMistakeBook()` 返回 `null`
- ✅ 所有云端方法自动降级到本地方法：
  - `saveMistake` → `_saveMistakeLocal`
  - `getMistakes` → `_getMistakesLocal`
  - `removeMistake` → `_removeMistakeLocal`
- ✅ 应用仍然可用（仅本地功能）
- ✅ 控制台输出降级日志

**提供情报**：
- 控制台日志（关注错误捕获、降级逻辑）
- 功能验证：错题保存、获取、删除是否仍能正常工作（本地模式）

**请开始执行。**
```

---

## 🎯 提示词 9: 继续 Module 2 验证（错题重复保存）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：Module 2 (刷题与解析) - 4/8 项完成

**下一步任务：TEST-2.6 - 错题重复保存的处理**

**前置条件**：
- 确保错题本中已有一道错题
- 再次遇到同一道题目（可以通过重新加载题库或手动触发）

**操作步骤**：
1. 在刷题页面，再次遇到已存在于错题本中的题目
2. 选择错误答案
3. 观察重复保存处理

**预期技术表现**：
- ✅ `saveToMistakes()` 检查本地缓存中是否存在相同题目
- ✅ 如果存在，获取已有的 `wrong_count` 值
- ✅ 新的 `wrong_count` = 原有值 + 1
- ✅ 云端保存时，可能创建新记录或更新已有记录（取决于去重逻辑）
- ✅ 本地缓存中更新已有记录的 `wrong_count` 值
- ✅ 控制台输出：`[do-quiz] 错题已保存，错误次数: X`

**提供情报**：
- 控制台日志（关注去重逻辑、`wrong_count` 更新）
- 数据验证：检查错题本中该题目的 `wrong_count` 是否正确递增

**请开始执行。**
```

---

## 🎯 提示词 10: 继续 Module 2 验证（学习统计）

```
你现在是我的"QA 测试工程师"。
请继续执行 Exam-Master V1.0 的功能验证。

**当前进度**：Module 2 (刷题与解析) - 4/8 项完成

**下一步任务：TEST-2.8 - 学习统计更新验证**

**操作步骤**：
1. 在刷题页面答完一道题目（正确或错误均可）
2. 检查学习统计数据

**预期技术表现**：
- ✅ 调用 `updateStudyStats()` 方法
- ✅ 获取当前日期：`new Date().toISOString().split('T')[0]`（格式：`YYYY-MM-DD`）
- ✅ 从本地存储读取：`storageService.get('study_stats', {})`
- ✅ 如果今日没有记录，初始化为 `{ "2025-01-22": 1 }`
- ✅ 如果今日已有记录，计数 +1：`{ "2025-01-22": 5 }`
- ✅ 保存回本地存储
- ✅ 首页或其他页面显示的学习统计数据更新

**提供情报**：
- 控制台日志（关注 `updateStudyStats`、存储读写）
- 数据验证：检查本地存储 `study_stats` 字段
- UI验证：首页或其他统计页面是否显示更新后的数据

**请开始执行。**
```

---

## 🎯 提示词 11: 开始 Module 7 验证（社交功能）

```
你现在是我的"QA 测试工程师"。
请开始验证 Exam-Master V1.0 的**Module 7: 社交功能模块**。

**任务：TEST-7.1 - 邀请好友功能验证**

**操作步骤**：
1. 进入"我的"Tab (`pages/profile/index.vue`)
2. 找到"我的好友们"区域
3. 点击右上角"邀请好友"按钮
4. 观察邀请弹窗 (`InviteModal`)

**预期技术表现**：
- ✅ 显示邀请弹窗
- ✅ 邀请码显示：`EXAM2026` 或动态生成
- ✅ 邀请链接显示：`https://exam-master.com/i/...`
- ✅ 点击"复制链接"按钮
- ✅ 链接成功复制到剪贴板
- ✅ 显示Toast："链接已复制"
- ✅ 关闭弹窗功能正常

**提供情报**：
- 控制台日志（关注弹窗显示、复制操作）
- UI观察：弹窗内容、按钮功能

**请开始执行。**
```

---

## 🎯 提示词 12: 开始 Module 8 验证（学习计划）

```
你现在是我的"QA 测试工程师"。
请开始验证 Exam-Master V1.0 的**Module 8: 学习计划模块**。

**任务：TEST-8.1 - 学习计划列表加载**

**操作步骤**：
1. 进入学习计划页面 (`pages/plan/index.vue`)
2. 观察数据加载

**预期技术表现**：
- ✅ `onLoad` 生命周期执行
- ✅ 从本地存储或云端读取计划列表
- ✅ 如果为空，显示空状态组件
- ✅ 如果有数据，显示计划列表卡片
- ✅ 每个计划卡片显示：名称、状态、日期、进度等

**提供情报**：
- 控制台日志（关注数据加载）
- UI观察：列表显示、空状态

**请开始执行。**
```

---

## 📋 使用说明

1. **选择提示词**：根据当前验证进度，选择对应的提示词
2. **复制粘贴**：将提示词完整复制到新会话
3. **执行测试**：按照提示词中的操作步骤执行
4. **反馈结果**：提供控制台日志和UI观察结果
5. **更新进度**：完成测试后，更新 `../reports/01_功能验证进度报告.md`

---

**提示词版本**：V1.0  
**最后更新**：2025-01-22
