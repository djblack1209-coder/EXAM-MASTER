# Trivy 安全扫描忽略配置
# 
# 此文件用于排除已知的误报或已接受的风险

# 忽略模板文件中的占位符（非真实密钥）
# secrets.yaml.template 使用 ${VARIABLE} 占位符，在 CI/CD 中动态替换
deploy/k8s/secrets.yaml.template

# 忽略示例配置文件（仅包含占位符）
.env.example
deploy/docker/.env.example

# 忽略监控配置模板（使用环境变量占位符）
deploy/monitoring/alertmanager.yml

# 已评估的 npm 依赖漏洞（uni-app 框架间接依赖，无法直接升级）
# GHSA-67mh-4wv8-2f99 - esbuild 开发服务器漏洞，仅影响 dev 模式，生产环境不受影响
# GHSA-x8qp-wqqm-57ph - vue-i18n XSS，项目未使用 i18n 的 escapeParameterHtml 功能
