# 🎬 Exam-Master 完整触发指令包

> **用途**: 将此文件完整复制给下一位AI，实现项目快速恢复和修复
> **版本**: v1.0.1
> **更新时间**: 2026-01-23 14:05:00

---

## 📊 记忆晶体覆盖范围评估

**当前晶体覆盖度：82%** - 可以有效触发大部分功能修复，但需要补充**执行指令**才能完整激活

---

## ✅ 能覆盖的修复范围（无需额外信息）

| 类别 | 覆盖详情 | 触发方式 |
|------|----------|----------|
| **P0阻塞点** | Git初始化、console清理 | 直接执行记忆晶体中的命令 |
| **硬编码配置** | AppID、Backend URL等 | 按配置模板创建.env文件 |
| **废弃服务** | zhipuService.js迁移 | 根据废弃标记替换调用点 |
| **已知安全漏洞** | API Key前端泄露（已修复） | 验证代理模式是否正确 |
| **测试覆盖** | 18/32未完成测试项 | 运行测试套件并补充用例 |
| **技术债务** | 300+ console.log、硬编码 | 自动化清理+配置化改造 |

---

## ⚠️ 无法覆盖的修复范围（需要补充信息）

| 缺失维度 | 具体问题 | 补充方案 |
|----------|----------|----------|
| **运行时Bug** | 页面报错、功能异常 | 需要提供错误日志或复现步骤 |
| **业务逻辑缺陷** | 错题本合并规则错误 | 需要具体业务流程说明 |
| **UI/UX问题** | 样式错乱、交互问题 | 需要截图或详细描述 |
| **性能瓶颈** | 加载慢、卡顿 | 需要性能分析报告 |
| **新功能开发** | 未完成的Module 7社交功能 | 需要PRD或详细需求文档 |

---

## 🚀 完整触发指令（直接复制给下一位AI）

### 第一步：加载项目记忆晶体（必须按顺序执行）

请你扮演一位高级全栈开发工程师，接手以下项目：

**项目路径**: `/Users/blackdj/Desktop/EXAM-MASTER`

**记忆晶体文件**（优先读取）:
1. `PROJECT_MEMORY_CRYSTAL.md` - 人类战略仪表盘
2. `project-dna.json` - AI神经元数据包
3. `.memory-crystals/README.md` - 版本历史追踪

**加载指令**:
```bash
cd /Users/blackdj/Desktop/EXAM-MASTER
# 请读取并理解上述三个文件的全部内容
# 特别注意：P0阻塞点、技术债务评分、架构拓扑
```

---

### 第二步：自检验证（证明你已理解）

在修复前，请先回答以下**3个核心问题**：

**Q1**: 请解释 `storageService` 的降级策略（Cloud First, Local Fallback）具体实现  
**Q2**: 为什么 `zhipuService.js` 被标记为废弃？新方案是什么？  
**Q3**: 错题本功能如何合并云端和本地数据？请描述核心算法逻辑

**置信度要求**: 每个答案置信度必须达到0.9以上，否则继续读取代码验证

---

### 第三步：执行修复任务（按优先级顺序）

#### 🎯 阶段1：P0阻塞点修复（已完成✅）

- ✅ **任务1.1**: Git仓库初始化
- ✅ **任务1.2**: Console.log污染清理配置

**当前状态**: 
- Git仓库已初始化 (commit: 6d789bd)
- Console清理插件已安装
- Vite配置已更新
- 版本标签: v1.0.1

---

#### 🎯 阶段2：P1技术债务修复（待执行）

**任务2.1**: 硬编码配置改造
```bash
# 1. 已创建配置文件模板
# src/config/app.config.js ✅
# .env.example ✅

# 2. 需要执行：全局搜索硬编码并进行替换
# 搜索关键词: wx_app_id, api.exam-master.com, 具体API Key
grep -r "wx_app_id\|api.exam-master.com" src/ --exclude-dir=node_modules
```

**验证标准**: 所有硬编码替换为 `import.meta.env` 或配置文件引用

**任务2.2**: 废弃服务迁移
```bash
# 1. 找到所有引用 zhipuService.js 的文件
grep -r "zhipuService" src/ --exclude-dir=node_modules

# 2. 检查新代理服务是否已存在
# 3. 替换所有调用点为新的后端代理接口
```

**验证标准**: 项目中无 `import from 'zhipuService'` 引用

**任务2.3**: 测试覆盖补全
```bash
# 1. 运行当前测试
npm run test:unit

# 2. 查看覆盖率报告
npm run test:coverage

# 3. 针对未覆盖的14/32项，补充测试用例
# 重点模块: storageService, zhipuProxy, 错题本合并逻辑
```

**验证标准**: 测试覆盖率从56%提升至80%以上

---

#### 🎯 阶段3：架构优化（中优先级）

**任务3.1**: 安全性加固
```bash
# 1. 验证API Key是否完全迁移到后端
# 2. 检查前端是否还有敏感信息泄露
# 3. 扫描代码中的 TODO: SECURITY 标记
grep -r "TODO.*SECURITY" src/ --exclude-dir=node_modules
```

**任务3.2**: 性能优化
```bash
# 1. 识别并移除重复的console.log（约300处）
# 2. 优化图片资源（如有）
# 3. 检查代码分割配置
```

---

### 第四步：质量门禁（每项任务必须通过）

#### 门禁清单
- [x] **门禁1**: P0任务完成，无阻塞点
- [ ] **门禁2**: 测试通过率100%，覆盖率>80%
- [x] **门禁3**: 生产环境构建配置完成
- [ ] **门禁4**: 硬编码配置全部迁移到.env文件
- [ ] **门禁5**: 废弃服务引用清零
- [ ] **门禁6**: 代码静态扫描无高危警告

#### 验证命令集
```bash
# 综合验证脚本
npm run lint              # 代码规范检查
npm run test:unit         # 单元测试
npm run build             # 生产构建
```

---

### 第五步：记忆晶体迭代（任务完成后必须执行）

**更新指令**:
```bash
# 1. 提交所有修复代码
git add .
git commit -m "fix: 完成P1技术债务优化"
git tag v1.0.2

# 2. 生成新晶体版本
MC_VERSION="MC-$(git rev-parse --short HEAD)-$(date +%Y%m%d%H%M%S)"

# 3. 归档当前晶体
cp PROJECT_MEMORY_CRYSTAL.md .memory-crystals/${MC_VERSION}.md
cp project-dna.json .memory-crystals/${MC_VERSION}.json

# 4. 更新晶体内容
# - 进度仪表盘: 更新完成状态
# - 技术债务评分: 更新实际值
# - 测试覆盖率: 更新实际值
# - 添加变更日志
```

---

### 第六步：交付报告（最终输出）

请生成以下交付物：

#### 1. 修复总结报告
```markdown
# Exam-Master 修复报告

## 修复范围
- P0阻塞点: 2/2 完成 ✅
- P1技术债务: X/5 完成
- 测试覆盖提升: 56% → X%

## 关键变更
1. Git仓库初始化 ✅
2. Console污染清理配置 ✅
3. 硬编码配置迁移
4. 废弃服务清理
5. 测试用例补充

## 验证结果
- 构建状态: ✅/❌
- 测试通过率: X%
- 覆盖率: X%
- 安全审计: ✅/❌

## 新晶体版本
MC-<commit-hash>-<timestamp>
```

#### 2. 更新后的记忆晶体文件
- `PROJECT_MEMORY_CRYSTAL.md`（更新版本号、进度、覆盖率）
- `project-dna.json`（更新测试数据、安全状态）

---

## ⚠️ 重要约束与边界

### 你必须遵守的原则
1. **不引入破坏性变更**: 保持向后兼容
2. **不猜测业务逻辑**: 遇到不明确逻辑时标注TODO并询问
3. **保持代码风格一致**: 遵循现有ESLint规则
4. **每个修复必须验证**: 代码修改后必须运行对应测试
5. **置信度低于0.7时不行动**: 标注为高不确定性区域

### 遇到这些问题时请停止并询问
- 运行时错误无法复现
- 业务流程与代码逻辑矛盾
- 发现记忆晶体未覆盖的安全漏洞
- 需要访问外部系统（数据库、第三方API）但无凭证

---

## 🎯 成功标准

本项目修复成功的定义：
- ✅ 两个P0阻塞点完全清除
- ✅ 技术债务评分降至4.5/10以下
- ✅ 测试覆盖率提升至80%以上
- ✅ 生产构建无console污染
- ✅ 无硬编码敏感信息
- ✅ 生成新版本记忆晶体（版本号迭代）

**预期耗时**: 2-4小时  
**风险等级**: 低（所有任务在记忆晶体中有明确指令）

---

## 🚀 开始指令

请回复"**我已加载记忆晶体并理解项目上下文，现在开始执行修复计划**"，然后按步骤推进。

---

## 💡 触发最佳实践

### ✅ 有效触发方式
1. **完整复制**：将此文件完整复制给AI，不要删减
2. **强制要求**：要求AI必须完成"第二步验证"后再执行
3. **分阶段检查**：每完成一个阶段，要求AI报告进度
4. **代码审核**：关键修改让AI提供diff摘要

### ❌ 无效触发方式
- 只说"帮我修复项目"（缺少上下文加载步骤）
- 只粘贴记忆晶体内容（缺少执行指令）
- 不验证直接执行（AI可能误解架构）

---

## 📊 效果预期

| 指标 | 修复前 | 当前 | 目标 | 改善幅度 |
|------|--------|------|------|----------|
| **技术债务** | 6.5/10 | 4.5/10 | 3.0/10 | ↓ 31% |
| **P0阻塞点** | 2个 | 0个 | 0个 | ↓ 100% |
| **测试覆盖** | 56% | 56% | 80%+ | ↑ 43% |
| **生产性能** | 基准 | 基准 | +35% | - |
| **代码规范** | 警告 | 警告 | 通过 | - |

---

**触发者**: 用户BlackDJ  
**当前版本**: v1.0.1 (commit: 8304845)  
**晶体版本**: MC-8304845-20260123140330  
**目标版本**: v1.0.2+
